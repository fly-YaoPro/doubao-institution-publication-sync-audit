---
name: doubao-institution-publication-sync-audit
description: 以客户发布表中的前一日真实发布为准，核对次日上午各机构多维执行表中对应合作的“进度”是否更新为“已发布”，长期记录机构更新及时率。用于“机构发布同步检查”“查机构进度同步”“哪些机构没更新已发布状态”等任务。
---

# 机构次日进度检查

## 公共业务配置

本 Skill 保留处理流程、例外和确认门禁，不单独维护客户表地址、周文档、机构色值或调度时间。先定位豆尼仓库（包含 `config/business-sources.json`），在仓库目录只读执行：

```powershell
node scripts/business-context.mjs --skill doubao-institution-publication-sync-audit
```

需要周执行文档时加 `--date YYYY-MM-DD`。阅读返回的来源、公共口径和本 Skill 写入边界；若运行时已注入 `runtime/business-context.json`，直接读取该快照，不另取一份不同版本。源缺失时停止相关操作，不能翻旧文档猜地址。手动和定时均调用原执行器，配置不授予新的写表或外发权限。

修改来源在仓库 `config/business-sources.json`，修改共用专项/色块/权威口径在 `config/business-policy.json`。调度以现有 Daemon 为准。脱离仓库独立打包时必须带上述配置和执行依赖、注明快照日期；不能把本 Skill 单独复制后视为可运行。Skill 自带命令仍在其原目录执行；包装器通过 `DOUNI_WORKSPACE` 或当前仓库目录定位执行器，不依赖固定盘符。

## 目标

考核机构是否在前一日内容真实发布后，按时把自己多维执行表中对应合作的“进度”更新为“已发布”。客户发布表是昨日真实发布事实来源，机构多维执行表是唯一考核目标；不再读取旧机构执行文档。

## 强制流程

1. 先完整读取 `references/match-and-scoring.md`。
2. 未指定日期时，默认检查上海时间前一自然日。
3. 真实发布清单取原抖音/小红书客户发布表及【独立端-内容发布】中发布日期等于业务日期、且达人名称和发布链接均完整的记录；按业务池+平台+作品 ID/规范化发布链接去重。独立端表没有机构色块，必须反查 13 张机构表确认归属。
4. 机构表从 公共配置 `sources.department` 指定的 Base 动态发现所有“机构 X 焦尾 执行表”，当前基线 13 张。逐条查找客户发布对应合作，并读取机构表实时“进度”；不得用全量表快照或旧机构执行文档替代。
5. 原客户表有机构色块时机构是第一层硬边界；独立端表无机构字段，须用作品/稿件/主页/达人证据反查机构，并限定机构表`项目/专项=独立端+PC端`及独立端内容方向。不得用达人历史机构猜测归属。
6. 匹配顺序固定为：作品 ID/GID → 规范化发布链接 → 同平台达人昵称+主页/稿件 → 同机构唯一当前合作。只有唯一命中才读取进度；多期合作或同分记录无法消歧时进入待核，禁止猜测。
   抖音 `/user/...` 链接如果查询参数中的 `group_id`、`from_gid`、`vid` 一致，必须提取该作品 ID 与 `/video/<id>` 比较；不能因为 pathname 是主页就误报链接冲突。短链必须展开后再比较。
   单元格显示文本、超链接目标、富文本类型或链接格式不一致时，必须读取单元格的真实富文本链接并规范化后再判断。显示格式异常但真实链接、作品 ID 或同机构唯一记录一致，应判为已同步并在审计中记录格式异常；不得仅凭单元格外观、CSV 展示值或链接类型不同误报 `conflict`。
7. 唯一命中的机构记录只有在“进度”明确等于“已发布”时才算同步完成；进度为空或仍为大纲、脚本、拍摄、审核、待发布等任何其他状态，均记为“进度未更新”。完全找不到对应合作记为“机构记录缺失”。两者均计入机构未及时更新。
8. 没有当日发布的机构标记为“当日无样本”，不得计算为 100% 及时率。
9. 每次正式运行写入长期审计账本；同一业务日期可多次复查，但保留各次运行时间、客户表 revision、机构表 revision 和差异。
10. 结论只发内部群或私聊；不得发客户群、供应商群，不得修改任何机构或客户业务表。豆尼只读检查。
11. 卡片必须展示：客户发布表昨日有效发布数、机构表已更新数、进度未更新/记录缺失数、待核数及 13 张机构表覆盖情况。

## 执行

试跑前一日：

```powershell
node scripts/run.mjs --compact
```

指定日期：

```powershell
node scripts/run.mjs --date 2026-08-17 --compact
```

脚本只读飞书，不回写业务表。`missing` 表示机构进度未更新或对应合作缺失；`ambiguous` 表示无法唯一确认合作，进入人工复核。

## 输出

结论必须简洁并可复核：

- 检查日期、客户表真实发布数、涉及机构数；
- 机构进度已更新数、未更新/记录缺失数、待核数；
- 按机构展示“应更新 / 已更新 / 未更新 / 及时率”；
- 未更新和待核逐条附达人、平台、发布链接、机构当前进度及机构多维表；
- 0 样本机构只列“当日无样本”，不参与排名。

正式群版本使用飞书 Card 2.0。不得输出原始 JSON、内部 source ID 或不可点击的技术标识。

## 运行约束

- 本 Skill 不定义执行时间；定时启停和时间调整由 Daemon 调度台管理。
- 与发布表更新、产能盘点保持独立运行；任一模块失败不得篡改其他模块的历史结果。
- 同一日期同一来源 revision 的相同结论不得重复发送，但仍可记录幂等命中。
